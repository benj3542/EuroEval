# Optional defaults (not required)

# Languages to include (comma-separated). Used at build AND run.
EUROEVAL_LANGS=da,en

# Tasks to include (comma-separated), e.g. "sentiment-classification,topic-classification"
EUROEVAL_TASKS=sentiment-classification

# Default batch size (EuroEval default is 32)
EUROEVAL_BATCH_SIZE=16
